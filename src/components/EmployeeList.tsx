// Session 1 stretch goal: render a responsive grid of EmployeeCard components.
//
// Requirements:
//   - Import the Employee type from "../types/employee"
//   - Import EmployeeCard from "./EmployeeCard"
//   - Define an EmployeeListProps interface with employees: Employee[] and onSelectEmployee: (id: number) => void
//   - Show "No employees found." when the array is empty
//   - Use employees.map() with key={employee.id} — never use array indices as keys
//   - Wire each card's onClick to () => onSelectEmployee(employee.id)
//   - Wrap the list in a <ul> with aria-label="Employee list"
//   - Grid classes: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4
//
// Reference: Session 1 slides — "Conditional Rendering + Lists"

export function EmployeeList() {
  return null;
}
